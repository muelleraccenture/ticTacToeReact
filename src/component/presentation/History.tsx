import {Button} from "react-native";
import React from "react";

export const History = ({}) => {

    return (
        <Button
            title={"Back To Home"}
            onPress={() => null}
        >
        </Button>
    )
}
